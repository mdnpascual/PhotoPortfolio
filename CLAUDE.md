# Claude Code — Project Instructions

## Memory & Context

- **Authoritative project memory:** `docs/memory.md` — read this at the start of every session.
- **Project file structure:** `docs/project-structure.md` — read this before exploring the codebase to avoid redundant searches.
- Do **not** rely on the internal auto-memory files (`.claude/projects/…`). Treat `docs/memory.md` as the single source of truth.

## Keeping Structure Up to Date

Whenever you add, rename, or delete a file, update `docs/project-structure.md` in the same response. No exceptions.
